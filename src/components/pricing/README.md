# Precios (`/precios`)

Al entrar, lo primero es **la comparativa de planes** (`LandingPricingSection variant="page"`), no un hero. El H1 vive en esa sección. Debajo: valor, FAQ y CTA.

**Kadesh AI** (`key: kadesh_ai`) va primero en cada tarjeta y se pinta con el degradado violeta–azul (`ai-urim-fill`). Está en todos los planes; el subtítulo y el FAQ lo dicen. No cambies el color a naranja: esa marca es de la IA.

Copy de features: el nombre y la descripción salen del API. Si cambia el texto del feature, alinea panel (`src/components/profile/ai/README.md`) y esta página.
